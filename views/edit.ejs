<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Post</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            background-color: rgba(165, 42, 42, 0.332);
        }
        .box {
            background-color: aliceblue;
            border-radius: 1rem;
            box-shadow: 0 0 5rem rgba(0, 0, 0, 0.475);
            padding: 3rem 4rem;
            display: flex;
            justify-content: center;
            flex-direction: column;
            margin-top: 7rem;
            border: .2px solid black;
        }
        button {
            cursor: pointer;
            margin-top: 1.3rem;
            height: 2rem;
            width: 10rem;    
        }
   
    </style>
</head>
<body>
    <div class="box">
    <h2>Edit Your Post</h2>
    <p>Post ID : @<%= post.id %></p>
    <p>User Name : <%= post.username %></p>
    <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
        <textarea name="content" id="" cols="90" rows="10"><%= post.content %></textarea><br>
        <button>sub</button>
        <button><a href="http://localhost:8080/posts">Cancle</a></button>
    </form>
</div>
</body>
</html>